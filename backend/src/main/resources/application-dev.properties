# Development Profile
spring.jpa.show-sql=false
logging.level.com.ticketbroker=DEBUG

# Database Configuration - Development
spring.datasource.url=jdbc:postgresql://${POSTGRES_DB_ADDRESS:192.168.1.151:5432}/ticketbroker_dev
spring.datasource.username=ticketbroker_dev
spring.datasource.password=${POSTGRES_PASSWORD:dev_password_change_me}
spring.datasource.driver-class-name=org.postgresql.Driver

# Logging Configuration - Development
logging.file.name=${LOG_FILE:/logs/application.log}

